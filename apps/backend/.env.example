# Core server
NODE_ENV=development
BACKEND_V2_PORT=3101
BACKEND_V2_HOST=0.0.0.0
LOG_LEVEL=info

# Data directory for DuckDB (config + time-series)
DATA_DIR=./data

# Time-series ingestion (true|false)
TIMESERIES_ENABLED=true

# Master key for AES-256-GCM encryption of service secrets at rest.
# Accepts base64 or hex 32-byte keys, or any string (hashed to 32 bytes).
# REQUIRED. If lost, all stored service secrets are unrecoverable.
#   Generate: `openssl rand -base64 32`
WATCHMAN_MASTER_KEY=

# Admin auth (used by the UI). Services are configured via the UI, not env.
AUTH_USERNAME=admin
AUTH_PASSWORD_HASH=

# Legacy: WATCHMAN_SERVICES_CONFIG or per-kind service env vars will be
# imported once on first boot into DuckDB, then ignored. Remove them after
# the first successful migration.
