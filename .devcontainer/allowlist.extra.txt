# Watchman project-specific egress extras (merged onto base-allowlist.txt by
# LockBox/sync.sh into the baked allowlist.txt).

# Anthropic / Claude Code (auth, auto-updater, docs beyond the base API).
platform.claude.com
claude.ai
code.claude.com
docs.claude.com
downloads.claude.ai

# OpenAI Codex — API-key traffic plus ChatGPT/device-code authentication.
api.openai.com
auth.openai.com
chatgpt.com

# npm
registry.npmjs.org

# GitHub container registry (releases / images).
ghcr.io
pkg-containers.githubusercontent.com

# Node (node-gyp headers for native modules at runtime)
nodejs.org

# safe-chain malware list
malware-list.aikido.dev

# Plugins: context7 MCP (npx @upstash/context7-mcp → context7 docs API)
context7.com

# Playwright browser binary downloads (E2E in apps/frontend)
cdn.playwright.dev
playwright.download.prss.microsoft.com
